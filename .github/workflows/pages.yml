name: Deploy Frontend (GitHub Pages)

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Build frontend
        working-directory: frontend
        env:
          VITE_BASE: /crypto-agent/
          VITE_GATEWAY_URL: ${{ secrets.PUBLIC_BASE_URL }}
        run: |
          npm install
          npm run build
          cp -f public-demo.html dist/public-demo.html || true
          # HiBT Token 书签助手：替换 Pages 主入口，便于手机扫/电脑收藏
          cp -f hibt-token.html dist/hibt-token.html
          cp -f hibt-import.html dist/hibt-import.html
          cp -f hibt-m.html dist/hibt-m.html
          cp -f hibt-m.html dist/index.html
          cp -f hibt-token.html dist/hibt-token.html
          ls -la dist

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Print frontend URL
        run: |
          echo "### Frontend entry" >> $GITHUB_STEP_SUMMARY
          echo "https://puito.github.io/crypto-agent/" >> $GITHUB_STEP_SUMMARY
          echo "HiBT Token: https://puito.github.io/crypto-agent/hibt-token.html" >> $GITHUB_STEP_SUMMARY
          echo "Demo: https://puito.github.io/crypto-agent/public-demo.html" >> $GITHUB_STEP_SUMMARY
