# 加密市场自主 Agent 系统  
## 完整微服务架构 + 模型训练 + 前端设计流程图

> 本文档是项目核心设计文档，包含：  
> 1. 微服务整体架构与数据流  
> 2. 在线运行（Chat + LLM）完整流程  
> 3. 离线训练与插件热加载流程  
> 4. 关键交互流程细化  
> 5. 前端双端设计建议（用户端 + 训练端）

---

## 一、微服务整体架构图

```mermaid
graph TB
    subgraph 前端层
        U[用户端<br/>图表 + Chat + 交易控制]
        T[训练端<br/>回测 + 参数优化 + 模型评测]
    end

    subgraph API 入口
        GW[API Gateway<br/>:8000]
    end

    subgraph 核心业务微服务
        CFG[Config Service<br/>:8002<br/>配置中心]
        CHART[Chart Service<br/>:8005<br/>绘图指令]
        AGENT[Agent Orchestrator<br/>:8006<br/>LLM 大脑]
        PLUGIN[Plugin Service<br/>:8003<br/>热插拔插件]
        BT[Backtest & Simulator<br/>:8004<br/>回测+模拟交易]
        DATA[Data Service<br/>:8001<br/>行情采集+数据集]
        MULTI[Multi-Agent Manager<br/>:8007<br/>矩阵测试]
    end

    subgraph 基础设施
        REDIS[(Redis<br/>消息总线 + 缓存)]
        HF[Hugging Face<br/>数据集仓库]
        LLM[Ollama / 云端 LLM]
    end

    U --> GW
    T --> GW
    GW --> AGENT
    GW --> CFG
    GW --> CHART
    GW --> BT
    GW --> PLUGIN
    GW --> DATA
    GW --> MULTI

    AGENT --> LLM
    AGENT --> CFG
    AGENT --> CHART
    AGENT --> BT
    AGENT --> PLUGIN
    AGENT --> DATA

    DATA --> HF
    PLUGIN --> BT
    MULTI --> BT
    MULTI --> PLUGIN

    CFG -.-> REDIS
    CHART -.-> REDIS
    AGENT -.-> REDIS
    DATA -.-> REDIS
```

---

## 二、在线运行核心流程（用户 Chat 对话）

### 2.1 整体时序图

```mermaid
sequenceDiagram
    participant User as 用户
    participant FE as 用户端前端
    participant GW as API Gateway
    participant Agent as Agent Orchestrator
    participant LLM as 大模型 (Ollama/云端)
    participant Config as Config Service
    participant Chart as Chart Service
    participant BT as Backtest Service
    participant Plugin as Plugin Service
    participant Data as Data Service

    User->>FE: 输入自然语言
    FE->>GW: WebSocket 发送消息
    GW->>Agent: 转发 + 上下文
    Agent->>LLM: Prompt + 可用 Tools 列表
    LLM-->>Agent: 返回 Tool 调用决策

    alt 修改配置
        Agent->>Config: 调用 update_config（预览模式）
        Config-->>Agent: 返回变更 Diff
        Agent->>FE: 展示「即将修改内容，是否确认？」
        User->>FE: 确认 / 取消
        FE->>Agent: 确认指令
        Agent->>Config: 真正执行更新
        Config-->>Agent: 成功 + 广播事件
    else 绘制图表
        Agent->>Chart: draw_fibonacci / draw_support 等
        Chart-->>Agent: 绘图对象已创建
        Chart->>FE: WebSocket 推送绘图指令
    else 回测 / 信号
        Agent->>Plugin: 获取当前策略插件
        Agent->>Data: 拉取历史/实时数据
        Agent->>BT: 执行回测或生成信号
        BT-->>Agent: 回测报告 / 信号结果
    end

    Agent->>LLM: 将执行结果喂回大模型
    LLM-->>Agent: 生成自然语言回复
    Agent->>FE: 流式返回文字 + 状态更新
    FE->>User: 显示回复 + 图表实时变化
```

### 2.2 配置修改「预览-确认-生效」流程

```mermaid
flowchart TD
    A[用户通过 Chat 提出配置修改] --> B[Agent 解析意图]
    B --> C[调用 Config Service<br/>preview 模式]
    C --> D[生成变更 Diff]
    D --> E[Agent 向用户展示预览]
    E --> F{用户选择}
    F -->|确认| G[调用 Config Service<br/>apply 模式]
    F -->|取消| H[放弃修改]
    F -->|直接生效| G
    G --> I[写入配置中心]
    I --> J[通过 Redis 广播变更事件]
    J --> K[所有相关服务 + 前端实时感知]
    K --> L[Agent 回复「已生效」]
```

---

## 三、离线训练与模型全生命周期

### 3.1 数据 → 训练 → 插件完整流程

```mermaid
flowchart TD
    subgraph 数据采集
        A1[Data Service 定时任务<br/>每 5 分钟] --> A2[拉取 Binance 5m K线]
        A2 --> A3[数据清洗 + 增量存储]
        A3 --> A4[特征工程<br/>RSI/KDJ/MACD/波动率等]
        A4 --> A5[生成带标签数据集]
        A5 --> A6[上传 Hugging Face<br/>版本化]
    end

    subgraph 模型训练（训练端）
        B1[从 Hugging Face 拉取数据集] --> B2[选择训练方式]
        B2 --> B3[参数网格/贝叶斯优化]
        B2 --> B4[训练轻量模型<br/>分类/回归/规则]
        B3 --> B5[产出最优参数文件]
        B4 --> B6[产出模型文件<br/>.pkl / .joblib / .onnx]
        B5 --> B7[本地回测验证]
        B6 --> B7
        B7 --> B8{评测是否通过}
        B8 -->|通过| B9[打包为插件]
        B8 -->|不通过| B2
    end

    subgraph 插件上线
        C1[上传插件到 Plugin Service] --> C2[注册元数据<br/>名称/版本/支持模式]
        C2 --> C3[热加载完成]
        C3 --> C4[可被 Agent / 回测 / 多分身使用]
    end

    A6 --> B1
    B9 --> C1
```

### 3.2 插件热插拔流程

```mermaid
sequenceDiagram
    participant Admin as 管理员/训练端
    participant Plugin as Plugin Service
    participant Agent as Agent Orchestrator
    participant BT as Backtest Service
    participant Multi as Multi-Agent

    Admin->>Plugin: 上传新插件文件 + 元数据
    Plugin->>Plugin: 校验接口兼容性
    Plugin->>Plugin: 动态加载到内存
    Plugin-->>Admin: 注册成功

    Note over Plugin: 广播插件变更事件

    Agent->>Plugin: 获取最新可用插件列表
    BT->>Plugin: 加载指定插件进行回测
    Multi->>Plugin: 同时加载多个插件做矩阵测试
```

---

## 四、多分身矩阵测试流程

```mermaid
flowchart TD
    A[用户/Agent 发起矩阵测试] --> B[Multi-Agent Manager]
    B --> C[从 Plugin Service 获取候选插件/参数组合]
    C --> D[创建多个并行任务]
    D --> E1[分身1: 插件A + 参数组1]
    D --> E2[分身2: 插件A + 参数组2]
    D --> E3[分身3: 插件B + 参数组1]
    D --> E4[分身N: ...]
    E1 --> F[并行调用 Backtest Service]
    E2 --> F
    E3 --> F
    E4 --> F
    F --> G[收集所有回测结果]
    G --> H[按胜率/收益/回撤排序]
    H --> I[返回对比报告给前端/Agent]
```

---

## 五、前端设计建议

### 5.1 用户端（User Client）整体布局

```mermaid
graph TB
    subgraph 用户端界面
        direction TB
        Header[顶部导航栏<br/>Logo + 当前模式显示 + 用户信息]
        
        subgraph 主体区域
            direction LR
            Left[左侧区域 60%]
            Right[右侧区域 40%]
        end
        
        subgraph Left内容
            Chart[价格图表区<br/>K线 + 指标 + 绘图层]
            Stats[底部统计信息栏<br/>胜率 / 盈亏 / 持仓 / 信号数]
        end
        
        subgraph Right内容
            ChatHeader[Chat 标题栏<br/>右上角：Chat设置 + 交易设置]
            ChatWindow[对话消息流]
            ChatInput[输入框 + 快捷指令]
            SignalHistory[交易/信号历史折叠面板]
        end
    end
```

#### 用户端功能分区说明

| 区域 | 功能点 | 交互说明 |
|------|--------|----------|
| **价格图表区** | K线、技术指标叠加、支撑压力位、斐波那契、信号标记 | 支持 Chat 驱动绘图，也可手动工具栏操作 |
| **Chat 对话区** | 自然语言交互 | 右上角两个按钮：Chat设置、交易设置 |
| **Chat设置** | 模型选择（Ollama/云端）、温度、系统提示词、确认模式开关 | 弹窗或抽屉 |
| **交易设置** | 当前模式（永续/事件/双模式）、启用插件、风险参数、自动平仓开关 | 弹窗，修改后走确认流程 |
| **信号/交易历史** | 历史信号列表、模拟成交记录、盈亏曲线 | 可展开/折叠，支持筛选 |
| **统计信息** | 实时胜率、累计收益、最大回撤、当前持仓、今日信号数 | 图表下方常驻 |

#### 用户端关键交互流程建议

1. **Chat 修改交易模式**：
   - 用户说「切换到事件合约30分钟」
   - Chat 显示预览卡片 → 用户点确认 → 顶部模式标签实时变化 + 图表刷新

2. **Chat 绘图**：
   - 用户说「从昨天低点到今天高点画斐波那契」
   - 图表自动出现斐波那契线，Chat 回复「已绘制」

3. **信号展示**：
   - 新信号产生时，图表上打点 + Chat 推送卡片 + 历史列表新增一条

---

### 5.2 训练端（Training Client）整体布局

```mermaid
graph TB
    subgraph 训练端界面
        direction TB
        THeader[顶部：数据集选择 + 当前插件/模型信息]
        
        subgraph 主体
            direction LR
            TLeft[左侧：数据与参数区]
            TRight[右侧：回测结果与评测区]
        end
        
        subgraph TLeft内容
            Dataset[数据集浏览器<br/>Hugging Face 版本列表]
            Params[参数配置面板<br/>网格搜索范围 / 模型超参]
            TrainBtn[开始训练 / 开始优化按钮]
        end
        
        subgraph TRight内容
            BacktestChart[回测权益曲线 + 信号标记图]
            Metrics[核心指标卡片<br/>胜率、盈亏比、回撤、夏普]
            Compare[多组参数/模型对比表]
            Export[导出为插件按钮]
        end
    end
```

#### 训练端核心功能

| 功能 | 说明 |
|------|------|
| **数据集管理** | 浏览 Hugging Face 上的版本化数据集，一键拉取到本地 |
| **参数测试** | 支持单组参数手动回测 + 网格/贝叶斯批量优化 |
| **模型评测** | 上传已有模型文件，针对指定时间段回测，输出完整报告 |
| **可视化对比** | 多组参数或不同模型的权益曲线叠加对比 |
| **一键转插件** | 评测通过后，直接打包并注册到 Plugin Service |

#### 训练端推荐工作流

```mermaid
flowchart LR
    A[选择数据集版本] --> B[设置参数搜索空间<br/>或上传模型文件]
    B --> C[执行回测 / 优化]
    C --> D[查看指标 + 权益曲线]
    D --> E{是否满意}
    E -->|否| B
    E -->|是| F[导出为插件]
    F --> G[自动注册到 Plugin Service]
    G --> H[可在用户端立即使用]
```

---

## 六、前后端协作关键接口建议（简要）

### 用户端主要依赖接口
- `WS /ws/chat`：Chat 流式对话
- `WS /ws/chart`：绘图指令推送
- `WS /ws/config`：配置变更广播
- `GET /api/v1/signals/history`：信号历史
- `GET /api/v1/stats`：统计信息
- `POST /api/v1/config/preview` & `/apply`：配置预览与生效

### 训练端主要依赖接口
- `GET /api/v1/datasets`：数据集列表（来自 Data Service）
- `POST /api/v1/backtest/run`：单次回测
- `POST /api/v1/backtest/optimize`：参数优化
- `POST /api/v1/plugins/register`：注册新插件
- `GET /api/v1/plugins`：已有插件列表

---

## 七、总结：核心实现逻辑一句话

**数据持续采集 → 训练端产出并验证插件 → 插件热加载到系统 → 用户通过 Chat 指挥 LLM → LLM 调用微服务完成配置、绘图、回测、信号 → 结果实时反馈到图表与对话。**

训练与在线推理彻底分离，保证系统稳定与可复用性。

---

文档版本：v1.0  
最后更新：2026-09-04
